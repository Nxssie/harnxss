# Secrets — never commit real credentials
shell/secrets.fish
*.local
*.secret
.env
.env.*
!.env.example
.credentials.json
fish_variables
auth.json

# Backups created by install.sh
*.bak.*

# Tool deps / runtime state
node_modules/

# LazyVim writes this when using :LazyExtras UI — extras are declared in lazy.lua instead
tools/nvim/lazyvim.json
